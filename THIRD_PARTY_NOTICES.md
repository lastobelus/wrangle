# Third-Party Notices

## Imported upstream basis

This repository includes work derived from:

- Project: `codeagent-wrapper-rs`
- Upstream repository: <https://github.com/localSummer/codeagent-wrapper-node>
- Imported path: `codeagent-wrapper-rs`
- License: MIT

The initial `wrangle` workspace was created by importing and then refactoring that MIT-licensed code into a new project layout with new documentation, security defaults, transport abstractions, and backend interfaces.

The upstream code remains attributable to its original authors and contributors. `wrangle` preserves that provenance rather than presenting the imported basis as originally authored by `lastobelus`.

## Notice

The imported upstream project declared MIT licensing in its published package metadata. This repository preserves MIT licensing and keeps a durable provenance record in `docs/provenance.md`.

